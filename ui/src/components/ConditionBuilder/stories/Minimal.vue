<!--
  Everything optional switched off — no card, no "Where", no overflow menu, no
  add affordance — for a panel that supplies its own frame and controls.
-->
<template>
	<div class="w-full max-w-3xl">
		<ConditionBuilder
			v-model="conditions"
			:fields="sampleFields"
			bordered="none"
			:columns="{ operator: 'minmax(0, 9rem)' }"
		>
			<template #where />
			<template #conjunction />
			<template #actions />
			<template #addCondition />
		</ConditionBuilder>
	</div>
</template>

<script setup lang="ts">
import { ref } from "vue";
import { ConditionBuilder } from "../index";
import type { ConditionGroup } from "../index";
import { sampleFields } from "./fields";

const conditions = ref<ConditionGroup>({
	conjunctions: ["and"],
	conditions: [
		{ fieldname: "status", operator: "equals", value: "Open" },
		{ fieldname: "subject", operator: "like", value: "refund" },
	],
});
</script>
